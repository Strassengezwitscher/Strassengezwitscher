/**
 * Class for the sensitive config, which overwrites values given in the production config.
 * It is changed in the production environment, else never used.
 */
export class SensitiveConfig {
    constructor() {}
}
